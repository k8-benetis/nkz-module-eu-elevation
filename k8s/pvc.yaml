apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terrain-data-pvc
  namespace: nekazari
  labels:
    module: eu-elevation
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
